## 1. Correction du formulaire CodePen

- [x] 1.1 Modifier `handleCodePenClick` dans `assets/js/main.js` pour passer `action`, `method`, `target` via l'option `attributes` de `DOMUtils.createElement`
- [x] 1.2 Modifier l'input hidden pour passer `type`, `name`, `value` via l'option `attributes`

## 2. Vérification

- [x] 2.1 Vérifier que le form est correctement créé avec les bons attributs (inspection navigateur ou testmanuel)
- [x] 2.2 Tester le clic sur "Open in CodePen" — CodePen s'ouvre avec le code pré-rempli
